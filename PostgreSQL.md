# PostgreSQL: реляционная база данных

## Общие вопросы

* Какие типы данных поддерживает `PostgreSQL`?

* `PostgreSQL` использует потоки или процессы?

* Для чего нужен план запроса? 

## Транзакции

* Что такое транзакция? Из чего состоит?

* Возможно ли частичное исполнение транзакции? Возможно ли откатить транзацию?

## Изоляция транзакций

* Рассшифруйте аббревиатуру `ACID`.

* Что такое изоляция данных и какие виды изоляций существуют?

* Как реализуется изоляция транзакций через `MVCC`?

* В чем отличия уровней изоляции `Read Uncommitted`, `Read Committed`, `Repeatable Read` и `Serializable`?

* Когда возникает чтение "грязных" данных?

* Когда возникают неповторяющиеся и фантомные чтения? Как их избежать?

## Индексы

* В чем отличия первичного ключа от внешнего?

* Какие индексы знаете и как они устроены?

* В чем отличия индексов на бинарных деревьях и хеш-таблицах?

* Какие методы доступа к индексам знаете?

* В чем отличия методов доступа к индексам `GiST`, `SP-GiST`, `GIN` и `BRIN`?

* Что произойдет при вставке строки содержащей `NULL` для индексируемого поля?

## Распределенное хранение

* Как устроены шардирование и репликация?

* Как работает асинхронная репликация между нодами? Как `WAL` логи используются при асинхронной репликации?

## Язык SQL

* Какие подмножества `SQL` знаете? Какие инструкции относятся к какому подмножеству?

* Какие типы соединений существуют? Как они работают?

* Какие типы представлений существуют?

* Что такое `Common Table Expression` и как оно используется?

* Что из себя представляют триггеры? Когда они срабатывают?

* Особенности отработки команды `TRUNCATE`? Работа триггеров и блокировка.

* Как составляется план запросов? Команда `EXPLAIN`.
